# During development the Vite proxy handles /api → localhost:3000 automatically,
# so you can leave this blank or unset for local dev.
#
# For production (Vercel), set this to your Render web service URL, e.g.:
# VITE_API_BASE_URL=https://yt-summarizer-api.onrender.com
VITE_API_BASE_URL=
